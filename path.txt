Exemple de path:
export PATH="$PATH:${HOME}/Documents/Projects/flutter/flutter/bin/"

Example publicar a github pages:
cd example
flutter build web --web-renderer canvaskit --base-href "/flutter_desktop_cupertino/gh-pages/example/"
cp -r ./build/web/* ../gh-pages/example/

python3 -m http.server 9000

flutter run -d macos --enable-impeller



flutter pub global activate dartdoc
flutter pub global run dartdoc:dartdoc --output gh-pages/doc/


em pots posar comentaris a aquesta classe, per tal de poder generar la documentació
posa-hi un exemple al principi que faci servir tots els paràmetres i comenta també les funcions
en anglès si us plau